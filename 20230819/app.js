/**
 * * 원시타입
 * * 원시타입은 값이 변하지 않는다. 불변성
 * ! 숫자
 * ! 문자
 * ! 불리언
 * ! null
 * ! undefined
 * ! symbol
 *
 */

/**
 * * 참조타입(객체타입)
 * * 객채타입은 값이 변경 가능한 타입이다. 객체도 불변성으로 만들어 주는 것이 좋다.
 * ! 객체, 함수, 배열
 * !
 * !
 */

// false
//  obj === 메모리주소 -> { 객체 만들기 }
//  obj2 === 메모리주소 -> { 객체 만들기 }
